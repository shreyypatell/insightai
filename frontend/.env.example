# Backend API base URL
# In development the Vite proxy rewrites /api → http://localhost:8000
# so you can leave this as an empty string locally.
# For Netlify production deployment set this to your Render backend URL:
# VITE_API_BASE_URL=https://your-backend.onrender.com
VITE_API_BASE_URL=
